import UIKit
import Sweeft

extension Image {
    func fetchAGZapCampusclassinit(_ CampusClassInit: String) {
        print(CampusClassInit)
    }
    func bind1WfBJCampusclassinit(_ CampusClassInit: String, to imageView: UIImageView,
              default image: UIImage?,
              mapping transform: @escaping (UIImage) -> UIImage = { $0 }) {
        print(CampusClassInit)
    }
    func bindwDb0uCampusclassinit(_ CampusClassInit: String, to barButton: UIBarButtonItem,
              default image: UIImage? = nil,
              mapping transform: @escaping (UIImage) -> UIImage = { $0 }) {
        print(CampusClassInit)
    }
}
